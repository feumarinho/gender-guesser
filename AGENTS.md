# gender-guesser

Serviço de inferência de gênero por nome (base Brasil.IO). Python/Flask :5060 (dev).

**Memória de IA unificada do workspace** (este repo faz parte do meta-repo SonusCordis):
leia `../AGENTS.md`, `../ARCHITECTURE.md` e `../ai-memory/` (STATE, CONVENTIONS, DEPLOY)
antes de trabalhar, e mantenha-os atualizados. Não criar arquivos de memória próprios da
ferramenta neste repo.

```bash
pip install -r requirements.txt && python api.py
```
